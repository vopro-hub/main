##############################################
# DJANGO CORE SETTINGS
##############################################
DEBUG=False
SECRET_KEY=replace_with_a_very_long_random_key
DJANGO_SETTINGS_MODULE=virtual_office.settings.production

##############################################
# DATABASE (PostgreSQL in Docker)
##############################################
DATABASE_NAME=workspace_db
DATABASE_USER=workspace_user
DATABASE_PASSWORD=supersecurepassword
DATABASE_HOST=db
DATABASE_PORT=5432

# For Postgres docker image
POSTGRES_DB=workspace_db
POSTGRES_USER=workspace_user
POSTGRES_PASSWORD=supersecurepassword

##############################################
# REDIS (Channels + Celery)
##############################################
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/1
CELERY_RESULT_BACKEND=redis://redis:6379/2
CHANNEL_LAYERS_BACKEND=redis://redis:6379/3

##############################################
# SIMPLE JWT CONFIG
##############################################
SIMPLE_JWT_ACCESS_TOKEN_LIFETIME=300
SIMPLE_JWT_REFRESH_TOKEN_LIFETIME=604800
JWT_COOKIE_SECURE=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

##############################################
# DOMAINS
##############################################
DOMAIN=https://yourdomain.com
BACKEND_URL=https://yourdomain.com
FRONTEND_URL=https://yourdomain.com

##############################################
# STATIC & MEDIA
##############################################
STATIC_ROOT=/vol/web/static
MEDIA_ROOT=/vol/web/media

##############################################
# EMAIL / EXTERNAL SERVICES
##############################################
SENDGRID_API_KEY=your-sendgrid-key
TWILIO_ACCOUNT_SID=your-twilio-sid# .env.example - copy to .env and fill secrets (DO NOT COMMIT .env to git)

# Core
DEBUG=False
SECRET_KEY=replace_with_a_strong_secret_key

# Backend settings
DJANGO_SETTINGS_MODULE=virtual_office.settings.production
BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000
DOMAIN=http://localhost

# Database
POSTGRES_DB=workspace_db
POSTGRES_USER=workspace_user
POSTGRES_PASSWORD=supersecurepassword
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Also duplicate to environment names your code might read:
DATABASE_NAME=${POSTGRES_DB}
DATABASE_USER=${POSTGRES_USER}
DATABASE_PASSWORD=${POSTGRES_PASSWORD}
DATABASE_HOST=${POSTGRES_HOST}
DATABASE_PORT=${POSTGRES_PORT}

# Redis / Channels / Celery
REDIS_URL=redis://redis:6379/0
CHANNEL_LAYERS_BACKEND=redis://redis:6379/3
CELERY_BROKER_URL=redis://redis:6379/1
CELERY_RESULT_BACKEND=redis://redis:6379/2

# JWT lifetimes (seconds)
SIMPLE_JWT_ACCESS_TOKEN_LIFETIME=300
SIMPLE_JWT_REFRESH_TOKEN_LIFETIME=604800

# External services
SENDGRID_API_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_WHATSAPP_NUMBER=

# OpenAI (rotate key now)
OPENAI_API_KEY=

# Static / media
STATIC_ROOT=/vol/web/static
MEDIA_ROOT=/vol/web/media

TWILIO_AUTH_TOKEN=your-twilio-token

##############################################
# OPENAI
##############################################
OPENAI_API_KEY=your_new_rotated_key  # Replace immediately!
